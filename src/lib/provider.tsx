// src/lib/provider.tsx

'use client';

// Tambahkan provider global Anda di sini, seperti Zustand, TanStackQuery, dsb

export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
